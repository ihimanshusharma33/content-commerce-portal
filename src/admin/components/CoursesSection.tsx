import React from "react";

const CoursesSection: React.FC = () => {
  return (
    <div>
      <h2 className="text-xl font-semibold mb-4">Courses</h2>
      <p>Manage all the courses available on the platform.</p>
    </div>
  );
};

export default CoursesSection;